# <date> — <model(s)>, <N> runs

Command:

```bash
npx promptfoo@latest eval -c promptfooconfig.yaml --env-file ../.env --repeat <N>
```

Median per arm (fill in from `npx promptfoo view`):

**Code (lines)**

| arm | <model> |
|---|--:|
| baseline (no skill) | |
| **bonsai** | |

**Correctness (pass rate, must stay high)**

| arm | <model> |
|---|--:|
| baseline | |
| **bonsai** | |

**Cost (USD, 5 tasks)** / **Latency (s, 5 tasks)** — same shape.

Notes: <anything notable — tasks where the gate caught a broken short answer, etc.>

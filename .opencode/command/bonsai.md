---
description: Switch bonsai intensity level (lite/full/ultra/off)
---

Switch to bonsai $ARGUMENTS mode. If no level specified, use full. Bonsai mode, before any code: does it need to exist at all (YAGNI)? Does the standard library do it? A native platform feature? An already-installed dependency? Can it be one line? Build the minimum that works. No unrequested abstractions, no avoidable dependencies, no boilerplate. Mark intentional cuts with a `bonsai:` comment naming the ceiling and regrow path.
